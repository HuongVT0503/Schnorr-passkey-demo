# --- Dependencies (Frontend & Backend) ---
node_modules/
package-lock.json
# Note: Keep package-lock.json if you want deterministic builds, 
# but ignore it if you find it causes conflicts in this specific "under construction" phase. 
# usually checking in package-lock.json is recommended.

# --- Environment Variables (CRITICAL SECURITY) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
backend/.env

# --- Build Outputs (Backend) ---
# We ignore 'dist' because these are compiled JS files from your TS source
backend/dist/
backend/build/

# --- Build Outputs (Frontend) ---
frontend/frontend/dist/
frontend/frontend/build/

# --- Temporary / Generated Files ---
# This is the massive text dump you generated, we shouldn't track this
full_project_code.txt

# --- Python Utilities (Root) ---
# We KEEP tree.py and dump.py, but ignore cache and compiled python
__pycache__/
*.py[cod]
*$py.class
.Python
env/
venv/

# --- Sensitive Data ---
cookies.txt
id_rsa
id_rsa.pub
*.pem

# --- Database (Prisma/Docker) ---
# If you use sqlite locally
*.sqlite
*.sqlite-journal
# Docker volumes usually persist data here
postgres-data/
mysql-data/
mongo-data/

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- IDE & OS Settings ---
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp


full_project_code.txt
dump.py
tree.py
full.txt

