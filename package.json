{
  "name": "schnorr-passkey-monorepo",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build:full",
    "start": "cd backend && npm start"
  }
}